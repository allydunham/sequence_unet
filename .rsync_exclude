# Exclude large PN files
proteinnet/*

# Exclude py scripts in models (and elsewhere) as these are checked into git
*.py

# Exclude old model runs
/model_archive

# Exclude models generally (only sync the training traces)
*model.tf/*
*model.h5
